## What

<!-- New theme? Bug fix? Tell us in a line or two. -->

## Theme checklist (delete if not a theme PR)

- [ ] One new file in `themes/`, registered in `themes/index.js` (2 lines)
- [ ] `npm test` passes locally
- [ ] `npm run preview` — checked my theme in `previews/index.html`, **both states** (demo + offline toggle)
- [ ] Screenshot of the demo card attached below
- [ ] `name` is kebab-case and matches the filename; `author` is my handle

### Screenshot

<!-- Drag the previews/<your-theme>.svg render (or a screenshot) here.
     Once Vercel builds this PR, reviewers can also open
     <preview-url>/api/card?theme=<your-theme>&demo  live. -->
